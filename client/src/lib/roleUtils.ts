// Utility functions for consumer trip planning app
// Legacy corporate travel features have been removed

export function isConsumerUser(): boolean {
  return true; // All users are consumers in this version
}
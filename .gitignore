node_modules
dist
.DS_Store
server/public
vite.config.ts.*
*.tar.gz

# Environment files
.env
.env.local
.env.development.local
.env.test.local
.env.production.local

# Error logs
errors.txt
ts_errors.txt
server_errors.txt
client_errors.txt

# Corporate/Enterprise features (archived)
/archive/corporate/

# Scripts (local development only)
scripts/
*.script.ts
*.script.js

# Conversation assets (Claude artifacts)
attached_assets/

# Uploads (keep directory structure)
# Template images are committed, user uploads are ignored
uploads/covers/user_*
uploads/thumbnails/user_*
!uploads/covers/.gitkeep
!uploads/thumbnails/.gitkeep
# Keep template images in git (any format)
!uploads/covers/cover_template_*.webp
!uploads/covers/cover_template_*.jpg
!uploads/covers/cover_template_*.jpeg
!uploads/covers/cover_template_*.png
!uploads/thumbnails/thumb_template_*